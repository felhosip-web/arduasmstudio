[package]
name = "wasm-core"
version = "0.1.0"
authors = ["AI Studio AVR8 Engine"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
